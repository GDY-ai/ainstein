- 所有工具函数均返回标准化的字典结构，成功时包含业务数据，失败时返回包含 'error' 键的字典。
- 统计类工具在执行前统一进行数据清洗，使用 `pd.to_numeric(..., errors='coerce').dropna()` 处理非数值或缺失值。
- 工具函数内部集成日志记录（`logger.error`），并在发生异常时捕获并转换为错误响应，避免向上抛出未处理异常。
- 每个工具在注册时均通过 `_build_schema` 定义符合 LLM 调用规范的 JSON Schema，明确标注必填项与参数描述。
- **科学计算**：使用 `pandas` 进行数据处理，`scipy` 执行统计检验（t-test, shapiro等），`numpy` 进行线性代数运算。
- **网络请求**：使用 `requests` 库进行 HTTP 通信，集成 DuckDuckGo, Wikipedia, arXiv 等公开 API。
- **可选依赖**：`pytrends` 用于 Google Trends 数据获取（在 `web_data.py` 中通过 try-except 处理导入）。
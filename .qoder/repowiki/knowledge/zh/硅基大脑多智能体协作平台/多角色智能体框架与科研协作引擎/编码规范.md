- 所有智能体思考产出均要求 LLM 输出严格格式的 JSON，并通过 `extract_json` 工具函数进行鲁棒性解析，支持 Markdown 代码块及混合文本提取。
- 智能体行为遵循“思考-持久化-发布事件”的闭环模式，`BaseAgent.think` 方法自动将新产生的认知元素写入数据库并发布对应的事件总线消息。
- 角色配置采用静态定义（`ROLES`）与数据库持久化相结合的机制，启动时通过 `init_framework` 确保默认角色同步至数据库并提供内存缓存加速读取。
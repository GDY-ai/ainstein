- 在涉及 D3 等直接 DOM 操作的场景中，统一使用 `useRef` 存储 Simulation、ZoomBehavior 及节点映射表，避免 React 重渲染导致的状态丢失。
- 采用声明式的 D3 Join 模式（`.data().join()`）处理节点与连线的增删改，并配合 CSS Transition 或 D3 `.transition()` 实现平滑的入场与退出动画。
- 通过预定义的常量映射表（如 `CE_COLORS`, `CE_LABELS`, `REL_LABELS`）集中管理业务枚举的视觉表现与文案，确保全模块 UI 的一致性。
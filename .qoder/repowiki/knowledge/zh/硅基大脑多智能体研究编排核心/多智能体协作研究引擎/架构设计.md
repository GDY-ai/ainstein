- 采用多智能体架构，包含`scientist`（战略规划）、`researcher`（任务执行）和`director`（复盘与记忆管理）三个核心角色。
- 通过`llm_client`统一封装对DashScope（Anthropic兼容接口）的调用，并提供鲁棒的JSON提取工具。
- 智能体作为业务逻辑层，依赖外部的`database`模块进行状态持久化，并调用`engines`模块执行具体的科研推理流程。
- 采用提示词模板驱动（从`prompts/`目录加载），实现业务逻辑与Prompt内容的解耦。
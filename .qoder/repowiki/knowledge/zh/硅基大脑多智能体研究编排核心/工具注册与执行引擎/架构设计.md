- 采用注册表模式（Registry Pattern），通过 `registry.py` 集中管理工具元数据与实现映射。
- 内部划分为数据统计（`stats.py`）、数据访问（`data_access.py`）和网络检索（`web_data.py`）三个功能子域。
- `dispatch` 函数作为统一入口，根据工具类型自动处理数据集加载逻辑或透传参数，实现调用方与具体实现的解耦。
- 所有工具遵循统一的 JSON Schema 定义规范，通过 `_build_schema` 辅助函数生成标准化的 LLM 工具描述。
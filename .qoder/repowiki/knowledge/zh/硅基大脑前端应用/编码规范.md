- 所有 API 请求统一通过 `api.ts` 中的 `request` 辅助函数发起，自动注入 `Authorization` 头并处理 401 未授权状态。
- 业务数据类型集中定义在 `types.ts` 中，并在 `api.ts` 和页面组件中通过 import 引用，保持类型一致性。
- 受保护的路由通过在 `App.tsx` 中包裹 `<RequireAuth>` 组件实现，逻辑是检查 `getToken()` 是否存在。
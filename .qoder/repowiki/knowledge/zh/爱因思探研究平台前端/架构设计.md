- 采用 Vite + React 18 + TypeScript 技术栈，通过 `react-router-dom` 实现客户端路由（基础路径 `/ainstein`）。
- 架构分层清晰：`api.ts` 封装统一的 RESTful 请求逻辑与错误处理；`types.ts` 定义全量业务数据接口；`pages/` 承载 Dashboard 与 ProjectDetail 等核心视图组件。
- 入口文件 `main.tsx` 负责挂载根组件并注入路由上下文，`vite.config.ts` 配置了构建输出目录与静态资源路径。